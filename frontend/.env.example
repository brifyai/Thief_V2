# API Configuration
# URL base de la API del backend (Express)
# En desarrollo, apunta al servidor local en el puerto 3000
# ⚠️ IMPORTANTE: NO incluir /api al final, se agrega automáticamente en cada endpoint
# Ejemplo correcto: http://localhost:3000
# Ejemplo incorrecto: http://localhost:3000/api (esto causará errores 404)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Development Token (opcional, solo para testing)
# NEXT_PUBLIC_DEV_TOKEN=dev-token-for-testing
